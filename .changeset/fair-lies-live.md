---
"crusher-ui-kit": patch
---

Update release and project-state docs to reflect the first public npm publish and the current automated release flow. Also fix the bundler contract by shipping `lit` as a runtime dependency instead of a dev-only dependency.
